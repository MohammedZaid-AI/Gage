# Gage configuration. Copy to .env and adjust as needed.

# SQLAlchemy database URL
DATABASE_URL=sqlite:///./storage/observations.db

# AI providers: "mock" works with no keys. Options: mock | gemini | openai
VISION_PROVIDER=mock
LLM_PROVIDER=mock

# API keys (only needed for non-mock providers)
GEMINI_API_KEY=
OPENAI_API_KEY=

# Where uploaded images are stored
IMAGE_DIR=./storage/images
